#include <stdlib.h>
#include "allocate.h"
#include "meshdata.h"
#include "vars.h"
void allocate(void){
    // put memory allocation for mesh in here
   // M = (meshdata*) malloc(N *sizeof(struct meshdata));
}
void deallocate(){
    // put destructor stuff here
}
